% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/longevity.R
\name{type_longevity}
\alias{type_longevity}
\title{Posterior-weighted type longevity}
\usage{
type_longevity(
  object,
  class_col = NULL,
  posterior_threshold = 0.1,
  sub_class = FALSE
)
}
\arguments{
\item{object}{A \code{sef_fit} object produced by \code{fit_sef()}.}

\item{class_col}{Optional. Column name to use as the class label. Defaults
to the \code{class} argument used in \code{fit_sef()}.}

\item{posterior_threshold}{Numeric in \code{[0, 1]}. Phases with mean
posterior weight below this threshold are excluded from the envelope
(default: 0.1).}

\item{sub_class}{Logical. If TRUE and \code{object$subclass} is set, the
class label is the cross factor \code{class:subclass} (default: FALSE).}
}
\value{
A data.frame with columns \code{class}, \code{longevity_min},
\code{longevity_max}, \code{longevity_span}, \code{dominant_phase},
\code{n_finds}, and a list-column \code{weight_matrix} containing
the per-phase posterior weights (length K).
}
\description{
For each cultural class (or sub-class), returns the temporal envelope of
the depositional phases in which the class has non-negligible posterior
membership. The longevity is the union of the per-phase chronological
envelopes for those phases whose mean posterior weight (averaged over the
finds of that class) exceeds \code{posterior_threshold}.
}
\examples{
x <- archaeo_sim(n = 90, k = 3, seed = 1)
fit <- fit_sef(x, k = 3, context = "context")
type_longevity(fit)
}
\seealso{
\code{\link{detect_intrusions}}, \code{\link{gg_longevity}}

Other diagnostics: 
\code{\link{as_phase_table}()},
\code{\link{detect_intrusions}()},
\code{\link{ese}()},
\code{\link{pdi}()},
\code{\link{phase_diagnostic_table}()},
\code{\link{predict_phase}()}
}
\concept{diagnostics}

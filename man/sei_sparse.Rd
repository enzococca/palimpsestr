% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sei.R
\name{sei_sparse}
\alias{sei_sparse}
\title{Compute SEI matrix with automatic sparsification}
\usage{
sei_sparse(
  data,
  coords = c("x", "y", "z"),
  chrono = c("date_min", "date_max"),
  class_col = "class",
  weights = c(ws = 1, wz = 1, wt = 1, wc = 1),
  eps = 1e-09,
  z_floor = 0.25,
  n_threshold = 1000
)
}
\arguments{
\item{data}{Input data.frame.}

\item{coords}{Character vector of coordinate column names (x, y, z).}

\item{chrono}{Character vector with minimum and maximum dating columns.}

\item{class_col}{Class column name.}

\item{weights}{Named numeric vector with components \code{ws}, \code{wz},
\code{wt}, \code{wc}.  Each component is normalised to \eqn{[0, 1]} before
weighting, so the weights represent relative importance.}

\item{eps}{Small value to avoid division by zero in spatial distance.}

\item{z_floor}{Minimum vertical denominator.}

\item{n_threshold}{Datasets larger than this trigger sparse mode (default: 1000).}
}
\value{
A numeric matrix (same as \code{sei_matrix}).
}
\description{
Wrapper around \code{\link{sei_matrix}} that automatically sets
\code{max_dist} when the dataset exceeds a size threshold,
using the 25th percentile of pairwise distances as the cutoff.
}
\examples{
x <- archaeo_sim(n = 50, k = 2, seed = 1)
S <- sei_sparse(x)
}
\seealso{
\code{\link{sei_matrix}}

Other SEI: 
\code{\link{local_sei}()},
\code{\link{sei_matrix}()}
}
\concept{SEI}

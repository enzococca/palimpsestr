% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.R
\name{cv_sef}
\alias{cv_sef}
\title{K-fold cross-validation for SEF model}
\usage{
cv_sef(data, k_values = 2:6, n_folds = 5, seed = 1, ...)
}
\arguments{
\item{data}{A data.frame with archaeological find data.}

\item{k_values}{Integer vector of candidate phase counts.}

\item{n_folds}{Number of cross-validation folds (default: 5).}

\item{seed}{Random seed for fold assignment.}

\item{...}{Additional arguments passed to \code{\link{fit_sef}}.}
}
\value{
A data.frame with columns \code{k}, \code{fold},
\code{train_loglik}, \code{test_loglik}, \code{train_pdi}.
}
\description{
Splits the data into folds, fits on training folds, and evaluates
log-likelihood on the held-out fold. Useful for comparing different
K values or weight configurations.
}
\examples{
\donttest{
x <- archaeo_sim(n = 100, k = 3, seed = 1)
cv <- cv_sef(x, k_values = 2:4, n_folds = 3)
# Mean test log-likelihood per K
aggregate(test_loglik ~ k, data = cv, FUN = mean)
}
}
\seealso{
\code{\link{compare_k}}, \code{\link{fit_sef}}

Other validation: 
\code{\link{adjusted_rand_index}()},
\code{\link{bootstrap_sef}()},
\code{\link{confusion_matrix}()},
\code{\link{optimize_weights}()}
}
\concept{validation}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{archaeo_sim}
\alias{archaeo_sim}
\title{Simulate an archaeological palimpsest dataset}
\usage{
archaeo_sim(n = 150, k = 3, seed = NULL, mixing = 0.08)
}
\arguments{
\item{n}{Number of observations (finds).}

\item{k}{Number of latent depositional phases.}

\item{seed}{Optional random seed for reproducibility.}

\item{mixing}{Proportion of observations to perturb spatially and
taphonomically, simulating post-depositional disturbance (0--1).}
}
\value{
A data.frame with columns: \code{id}, \code{x}, \code{y}, \code{z},
\code{context}, \code{date_min}, \code{date_max}, \code{class},
\code{taf_score}, \code{true_phase}.
}
\description{
Generates a synthetic excavation dataset with known latent phases,
controlled spatial clustering, and configurable inter-phase mixing.
}
\examples{
easy <- archaeo_sim(n = 100, k = 3, mixing = 0.05, seed = 1)
table(easy$true_phase)

hard <- archaeo_sim(n = 200, k = 4, mixing = 0.50, seed = 1)
table(hard$true_phase)
}
\seealso{
\code{\link{fit_sef}} for fitting the SEF model to the output.
}
\concept{simulation}
